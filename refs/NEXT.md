# NEXT — after Claude muted-read PASS on the Menace cabin plate

Do not start this until Claude PASSes `refs/ref_menace_cabin_candidate.png`
(take 16) and a promote PR swaps `pink_menace_interior` off `ref_cockpit.jpg`.

No Act I–III replay. Do not reopen IV-026 (PR #72 take-110 stays seeded).

## 1. Promote the plate

Follow `refs/CANDIDATE_MENACE_CABIN.md` → After Claude PASS. Lock file,
`LOCKS.md` winners row, `pack/09_REF_MAP.json`, pack/03 live **positive**
string, `scripts/compile-prompt.js` attachment / Menace cabin match clause.

Pinned decisions travel with the lock:

- Flat painted-metal period dash; single nacelle; unbranded wheel; floor shifter
- **pedal_count: 3** (manual)
- Coarse Menace panel mesh — not Encore’s fine full-windshield grid
- Plate default: nacelle angled away (no glyphs). Card JSON wins if it names a needle.

## 2. Regen IV-027 first (hold until PASS)

Card JSON is sole brief authority (`cards/IV-027.json` on the citation-fixed
line). `POV_MIRROR_REAR` / `camera_is_the_lesson`. **Space With No Space.**

**Keep from take-11:** interior-mirror composition — wide weathered glass
filling the upper frame; truck **already on the bumper** in the rear glass
(grille-dominant: horizontal bars + bumper; no roof / cab / bed / wheels);
ice beads; no driver face.

**Drop into the Menace cabin plate:** coarse panel mesh around the mirror
(not a fine full-windshield grid); flat painted-metal pink dash / roof strip
if they crop the bottom; unbranded wheel / single nacelle only if they crop
in — nacelle angled away unless the brief names a needle; **no tablet, no
touchscreen.** Attach the promoted cabin plate, not `ref_cockpit.jpg`.

**Daylight:** ice at dawn is pale winter daylight, not Act VII night. Compiler
appends `DAYLIGHT_NEGATIVE` unless `variation.time_of_day` is night / dusk.
Do not let downtown lamps pull a full-night key.

## 3. Then IV-028, then IV-030

- **IV-028:** `POV_COCKPIT`, overcast midday, both cats on the passenger seat,
  cluster at 0 (card JSON — one period-correct dial, needle on 0), mesh, parked.
  Same cabin plate. No screen.
- **IV-030:** exterior `POV_TOPDOWN_PHOTO`, **clear cold morning** — not night.
  Wave-two takes 5–6 went full night (blue-black city, streetlights). Daylight
  negative is the lock.

## Not this queue

IV-026 reseed. Act I–III playthrough. Regenerating 027 / 028 / 030 before
Claude PASSes the plate.
